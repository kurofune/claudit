name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: gofmt
        run: |
          drift=$(gofmt -l .)
          if [ -n "$drift" ]; then
            echo "gofmt drift detected in:"
            echo "$drift"
            echo "Run 'gofmt -w .' locally to fix."
            exit 1
          fi
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - run: go vet ./...
      # -race catches the data races a plain run silently misses; the
      # serve daemon is the concurrency-heavy path this guards.
      - run: go test -race ./...
  staticcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      # @latest keeps pace with new Go releases without a pinned-version
      # bump ritual; the tool never touches go.mod.
      - run: go run honnef.co/go/tools/cmd/staticcheck@latest ./...
  # Unit tests for the SPA's pure JS logic (Node's built-in test runner,
  # no npm dependencies). OS-independent, so one runner is enough.
  js:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - run: npm test

### Testing policy — pointer

The testing policy is user-global: `~/.agents/AGENTS.md` § Testing Policy (backend and frontend-logic TDD via `/tdd-canon`; only an explicit "skip TDD" overrides it). This file exists because the shipped agent templates cite this path.

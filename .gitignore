# Generated reports
cost.md
report.html
*.html
# Source HTML under web/ (the SPA shell) is intentionally tracked —
# the *.html rule above exists to keep generated `report.html` out
# of the repo, not to ignore source.
!web/*.html

# Build artifacts
/claudit
/claudit.exe
/dist/

# Session handoff notes (per-session, not shared)
HANDOFF.md

# Local planning notes (not versioned)
roadmap_v2.md
todo.md

# Misc
.DS_Store